import { DropdownToStringPipe } from './dropdown-to-string.pipe';

describe('DropdownToStringPipe', () => {
  it('create an instance', () => {
    const pipe = new DropdownToStringPipe();
    expect(pipe).toBeTruthy();
  });
});
