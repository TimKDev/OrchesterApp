import { SelectTerminsFromDatePipe } from './select-termins-from-date.pipe';

describe('SelectTerminsFromDatePipe', () => {
  it('create an instance', () => {
    const pipe = new SelectTerminsFromDatePipe();
    expect(pipe).toBeTruthy();
  });
});
