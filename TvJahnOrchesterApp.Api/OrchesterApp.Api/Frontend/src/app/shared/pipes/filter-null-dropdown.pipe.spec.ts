import { FilterNullDropdownPipe } from './filter-null-dropdown.pipe';

describe('FilterNullDropdownPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterNullDropdownPipe();
    expect(pipe).toBeTruthy();
  });
});
