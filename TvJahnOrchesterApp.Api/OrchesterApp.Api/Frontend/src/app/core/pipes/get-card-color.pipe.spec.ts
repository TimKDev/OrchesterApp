import { GetCardColorPipe } from './get-card-color.pipe';

describe('GetCardColorPipe', () => {
  it('create an instance', () => {
    const pipe = new GetCardColorPipe();
    expect(pipe).toBeTruthy();
  });
});
